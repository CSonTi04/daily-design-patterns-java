package strategy;
/**
 * Strategy participant, declares an interface common to all supported strategy algorithms.
 */
public abstract class CookStrategy {
    public abstract void cook(String food);
}
